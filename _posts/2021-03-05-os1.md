---
title : "CS-운영체제-01"
layout: collection
permalink: /cstil/1
collection: cstil
entries_layout: grid
classes: wide
category : 
    - 운영체제
tag : 
    - 운영체제
toc : true
---

# PART 1. 운영체제와 컴퓨터

> 참고서적: 쉽게 배우는 운영체제, 저자: 조성호

# CHAPTER 01. 운영체제의 개요

## 01. 운영체제의 소개.

- 일상생활 속의 운영체제
    - 운영체제는 일반 컴퓨터나 노트북의 전원을 켜면 가장 먼저 만나게 되는 소프트웨어로, 대표적인 예는 개인용 컴퓨터에서 사용하는 윈도우와 맥 os가 있고 대형 컴퓨터에서 사용하는 유닉스와 리눅스 등이 있다.
    - 모바일에서 사용하는 운영체제는 모바일 운영체제라고 하고, 애플의 iOS와 구글의 안드로이드가 있다.
    - CPU 성능이 낮고 메모리 크기도 작은 시스템에 내장하도록 만든 운영체제를 임베디드 운영체제 또는 임베디드 시스템이라고 한다. 임베디드 운영체제도 운영체제 중의 하나이다.
        - 임베디드 운영체제는 CPU의 성능이 낮고 메모리 크기가 작은 기계에 설치되기 때문에 일반 운영체제에 비해 몇 가지 기능이 빠져있다.
        - 임베디드 운영체제를 사용하는 기계와 그렇지 않은 기계는 많은 차이가 있다. 임베디드 운영체제가 있는 기계는 기능을 계속 향상할 수 있다.

### 운영체제가 있는 기계와 없는 기계의 차이

- 운영체제가 없는 기계는 다른 기능을 추가하거나 성능을 향상시킬 수 없기 때문에 처음에 설계한 대로만 사용할 수 있다.
- 운영체제가 있는 기계는 기능 추가, 성능 변경이 가능하다. 이러한 기계를 프로그램이 가능한 기계라고 한다.

### 운영체제는 성능을 향상하는데만 사용?

- 성능향상뿐 아니라 자원 관리도 가능하다.

### 운영체제는 사용자에게 인터페이스를 제공하고 응용 프로그램에게 컴퓨터 자원을 골고루 배분한다.

### 펌웨어 == 운영체제

### 운영체제의 정의

- 운영체제는 사용자에게 편리한 인터페이스 환경을 제공하고 컴퓨터 시스템의 자원을 효율적으로 관리하는 소프트웨어이다.

### 운영체제의 역할

- (효율성)자원관리: 자원 분배, 자원 회수, 같은 자원을 관리하더라도 효율적으로 관리. 적은자원을 사용해서 결과를 빨리 보여주기.
- (안정성)자원보호:
- (확장성)하드웨어 인터페이스 제공: 복잡한 과정없이 다양한 장치를 사용할 수 있도록 해준다. CPU, 메모리, 키보드, 마우스 등등
    - 하드웨어 인터페이스 지원은 드라이버를 컴퓨터에 설치해야 한다.
    - 드라이버는 하드웨어 장치와 상호작용하기 위해 만들어진 컴퓨터 프로그램으로 장치 드라이버, 디바이스 드라이버, 장치 제어기, 소프트웨어 드라이버라고도 부른다.
- (편리성)사용자 인터페이스 제공

### 일괄 작업 시스템 또는 일괄 처리 시스템

- 천공카드 리더시대 일때..
    - 모든 작업을 한꺼번에 처리해야 하고 프로그램 실행 중간에 사용자가 데이터를 입력하거나 수정하는 것이 불가능하다.
        - 이러한 시스템을 일괄 작업 시스템(batch job system) 또는 일괄 처리 시스템(batch processing system)이라고 부른다.
- 일괄 작업 시스템은 운영 체제 전반에 걸쳐 자주 인용되는 시스템이니 꼭 기억!!

### 다중 프로그래밍

- 하나의 CPU로 여러 작업을 동시에 실행하는 기술.

### 시분할 시스템(time sharing system), 다중 작업(multitasking) 시스템

- 여러 작업을  조금씩 처리하여 작업이 동시에 이루어지는 것처럼 보이게 한다.
- CPU 사용시간을 잘게 쪼개어 작업들에 나누어줌으로써 모든 작업이 동시에 처리되는 것처럼 보인다.
- 이때 잘게 나뉜 시간 한 조각을 타임 슬라이스(time slice) 또는 타임 퀀텀(time quantum)이라고 한다.

### 데몬

- 클라이언트/서버 시스템에서는 서버가 멈추지 않고 계속 작동하여 클라이언트의 요청을 처리해야 하는데, 이렇게 멈추지 않고 계속 작동하는 프로그램을 데몬(daemon)이라고 한다. 보통은 데몬을 가진 컴퓨터를 서버라고 부르며, 웹 데몬이 설치된 컴퓨터는 웹 서버, FTP 데몬이 설치된 컴퓨터는 FTP 서버, 이메일 데몬이 설치된 컴퓨터는 이메일 서버라고 한다. 웹 데몬의 역할을 하는 프로그램으로는 아파치 톰캣, IIS 등이 있다.

### IDC = Internet Data Center

### 커널(Kernel)

- 커널은 프로세스 관리, 메모리 관리, 저장장치 관리와 같은 운영체제의 핵심적인 기능을 모아놓은 것으로 자동차에 비유하면 엔진이다.

### 인터페이스

- 커널에 사용자의 명령을 전달하고 실행 결과를 사용자에게 알려주는 역할.